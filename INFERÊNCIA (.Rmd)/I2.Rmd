---
output: html_document
---
#ESTATÍSTICA DESCRITIVA
```{r, include=FALSE}
library("mime")
library("stringi")
library("magrittr") 
library("Rcpp")
library("evaluate")
library("digest")
library("formatR")
library("highr")
library("markdown")
library("stringr")
library("yaml")
library("bitops")
library("caTools")
library("htmltools")
library("knitr")
library("rmarkdown")
library("e1071")
library("fBasics")
library("DAAG")
library("MASS")
library("fdth")
library("dplyr")
library("moments")
library("qcc")
library("rafalib")
library("reshape2")
library("survey")
library("raster")
library("knitr")
```

##QUESTÃO I2
A tensão de ruptura dos cabos produzidos por um fabricante apresenta a média de 1800kg e o desvio padrão de 100kg. Mediante nova técnica no processo de fabricação, proclamou-se que a tensão de ruptura pode ter aumentado. Para testar essa declaração, ensaiou-se uma amostra de 50 cabos, tendo-se determinado a tensão média de ruptura de 1850kg.

a)Pode-se confirmar a declaração com nível de significância 0,01?

###Resposta letra a)
Utilizou-se o teste Z, uma vez que n>30. Para o nível de significância de 0,01, a regra de decisão é:

Rejeita-se $H_{0}$ se z > $z_{0,01}$ = 2,33

Falha-se em rejeitar $H_{0}$ se z < 2,33

Desta forma as Hipóteses são:

$H_{0}:\mu$ = 1800 (não houve modificação da tensão de ruptura)

$H_{1}:\mu$ > 1800 (houve modificação da tensão de ruptura)

Como supõe-se que o desvio padrão não se tenha modificado, tem-se que $\sigma$ = 100.

A estatística a ser calculada é:

$$
\begin{aligned}
z =\frac{(\bar{X}-\mu_{0})}{\sigma / \sqrt[2]{n}}
\end{aligned}
$$

$$
\begin{aligned}
z =\frac{(1850-1800)}{100 / \sqrt[2]{50}} = 3.55
\end{aligned}
$$

*O cógido abaixo reflete os cálculos apresentados acima.*
```{r}
M     <- 1800
dp    <- 100
n     <- 50
x     <- 1850
alpha <- 0.01

z.alpha <- abs(qnorm(alpha))
z0      <- (x - M) / (dp/sqrt(n))

if (z0 > z.alpha) {
  print("Rejeitar Hipótese Nula")
} else {print("Falhar em Rejeitar Hipótese Nula")}
```

Sim. É possivel afirmar que a média da tensão de ruptura dos cabos aumentou, com 99% de confiança, em todos os intervalos considerados,para todas as amostras, pois a hipótese nula, a qual considera que a média é igual a 1800 kg, foi regeitada e, $z_{0}$ > $z_{\alpha}$.